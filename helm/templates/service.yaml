apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubevue.fullname" . }}
  labels:
    {{- include "kubevue.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubevue.selectorLabels" . | nindent 4 }}
